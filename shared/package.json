{
  "name": "announsing-shared",
  "private": true,
  "version": "0.0.1",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/esmodule/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.esmodule.json",
    "build.watch": "tsc-watch -p tsconfig.build.json --onSuccess 'tsc -p tsconfig.esmodule.json'",
    "test": "jest",
    "test.watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "type-fest": "^0.21.1"
  }
}
