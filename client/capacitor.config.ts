import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.announcing.client1',
  appName: 'Announcing♪',
  webDir: 'cap',
  bundledWebRuntime: false,
};

export default config;
