{
  "name": "announsing-console",
  "private": true,
  "watch": {
    "copy-shared": {
      "patterns": [
        "../shared-ui/ap-components"
      ],
      "extensions": "ts,tsx,scss"
    }
  },
  "scripts": {
    "build": "rm -rf www && stencil build",
    "serve": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "copy-shared": "node copy-shared.js",
    "copy-shared.watch": "npm-watch copy-shared",
    "start": "run-p copy-shared.watch serve"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^2.4.1",
    "@stencil/core": "^2.4.0",
    "@stencil/sass": "^1.4.1",
    "@stencil/store": "^1.4.1",
    "@types/pica": "^5.1.2",
    "announsing-shared": "file:shared",
    "bootstrap-icons": "^1.4.0",
    "date-fns": "^2.18.0",
    "destyle.css": "^2.0.2",
    "firebase": "^8.2.9",
    "fs-extra": "^9.1.0",
    "intersection-observer": "^0.12.0",
    "npm-run-all": "^4.1.5",
    "npm-watch": "^0.9.0",
    "pica": "^6.1.1"
  }
}
